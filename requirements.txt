# Pinned to the versions verified in your venv where applicable.
# Live IBKR connectivity is best on Python 3.11-3.13 (see README; your venv is 3.14).
nautilus_trader==1.229.0
nautilus-ibapi==10.45.1
numpy>=1.26
pandas>=2.1
scikit-learn>=1.4
hmmlearn>=0.3       # Gaussian HMM regime feature (models/regime.py)
optuna>=3.6
fastapi>=0.115      # dashboard API (quant/api/)
uvicorn>=0.30        # ASGI server for the dashboard API
